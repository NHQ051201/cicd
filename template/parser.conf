[PARSER]
    Name        docker
    Format      json
    Time_Key    time
    Time_Format %Y-%m-%dT%H:%M:%S %z

[PARSER]
    Name container_parser
    Format regex
    Regex ^\/(?<PROJ>[^-]+)-(?<ENV>[^.]+)-(?<APP>[^.]+)
